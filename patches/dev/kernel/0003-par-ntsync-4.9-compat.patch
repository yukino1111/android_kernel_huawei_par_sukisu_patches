diff --git a/drivers/misc/Kconfig b/drivers/misc/Kconfig
index 9360e6ebb..8f0b3501f 100644
--- a/drivers/misc/Kconfig
+++ b/drivers/misc/Kconfig
@@ -4,6 +4,13 @@
 
 menu "Misc devices"
 
+config NTSYNC
+	bool "NT synchronization primitive emulation"
+	default n
+	help
+	  This driver provides kernel support for emulation of Windows NT
+	  synchronization primitives.
+
 config SENSORS_LIS3LV02D
 	tristate
 	depends on INPUT
diff --git a/drivers/misc/Makefile b/drivers/misc/Makefile
index 0d5b9e189..fafbdd2a2 100644
--- a/drivers/misc/Makefile
+++ b/drivers/misc/Makefile
@@ -23,6 +23,7 @@ obj-$(CONFIG_ENCLOSURE_SERVICES) += enclosure.o
 obj-$(CONFIG_KGDB_TESTS)	+= kgdbts.o
 obj-$(CONFIG_SGI_XP)		+= sgi-xp/
 obj-$(CONFIG_SGI_GRU)		+= sgi-gru/
+obj-$(CONFIG_NTSYNC)		+= ntsync.o
 obj-$(CONFIG_CS5535_MFGPT)	+= cs5535-mfgpt.o
 obj-$(CONFIG_HP_ILO)		+= hpilo.o
 obj-$(CONFIG_APDS9802ALS)	+= apds9802als.o
diff --git a/drivers/misc/ntsync.c b/drivers/misc/ntsync.c
index 528dda6f9..028ace44f 100644
--- a/drivers/misc/ntsync.c
+++ b/drivers/misc/ntsync.c
@@ -14,7 +14,7 @@
 #include <linux/miscdevice.h>
 #include <linux/module.h>
 #include <linux/mutex.h>
-#include <linux/overflow.h>
+#include <linux/compat.h>
 #include <linux/sched.h>
 #include <linux/sched/signal.h>
 #include <linux/slab.h>
@@ -26,6 +26,52 @@
 #include <linux/path.h>
 #include <linux/xattr.h>
 #include <linux/version.h>
+#include <linux/uaccess.h>
+
+#ifndef atomic_try_cmpxchg
+static inline bool atomic_try_cmpxchg(atomic_t *value, int *old, int new)
+{
+	int old_value = atomic_cmpxchg(value, *old, new);
+
+	if (old_value == *old)
+		return true;
+	*old = old_value;
+	return false;
+}
+#endif
+
+#if LINUX_VERSION_CODE < KERNEL_VERSION(4, 15, 0)
+#define lockdep_assert(condition) do { } while (0)
+#define lockdep_is_held(lock) 1
+#define LOCK_STATE_NOT_HELD 0
+#endif
+
+#ifndef check_add_overflow
+#define check_add_overflow(a, b, result) ({ \
+	typeof(a) __a = (a); \
+	typeof(b) __b = (b); \
+	*(result) = __a + __b; \
+	*(result) < __a; \
+})
+#endif
+
+#ifndef array_size
+#define array_size(count, size) ((count) * (size))
+#endif
+
+#ifndef struct_size
+#define struct_size(pointer, member, count) \
+	(sizeof(*(pointer)) + sizeof((pointer)->member[0]) * (count))
+#endif
+
+#ifndef compat_ptr_ioctl
+static long compat_ptr_ioctl(struct file *file, unsigned int cmd,
+								 unsigned long arg)
+{
+	return file->f_op->unlocked_ioctl(file, cmd,
+								  (unsigned long)compat_ptr(arg));
+}
+#endif
 
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 3, 0)
 #include <linux/mnt_idmapping.h>
diff --git a/include/linux/compiler_types.h b/include/linux/compiler_types.h
new file mode 100644
index 000000000..d3467edd7
--- /dev/null
+++ b/include/linux/compiler_types.h
@@ -0,0 +1,6 @@
+#ifndef __LINUX_COMPILER_TYPES_H
+#define __LINUX_COMPILER_TYPES_H
+
+#include <linux/compiler.h>
+
+#endif
diff --git a/include/linux/minmax.h b/include/linux/minmax.h
new file mode 100644
index 000000000..55a4be68e
--- /dev/null
+++ b/include/linux/minmax.h
@@ -0,0 +1,6 @@
+#ifndef _LINUX_MINMAX_H
+#define _LINUX_MINMAX_H
+
+#include <linux/kernel.h>
+
+#endif
diff --git a/include/linux/overflow.h b/include/linux/overflow.h
new file mode 100644
index 000000000..6bc5f0e96
--- /dev/null
+++ b/include/linux/overflow.h
@@ -0,0 +1,26 @@
+#ifndef _LINUX_OVERFLOW_H
+#define _LINUX_OVERFLOW_H
+
+#include <linux/compiler.h>
+#include <linux/kernel.h>
+
+#ifndef check_add_overflow
+#define check_add_overflow(a, b, d) ({ \
+	typeof(a) __a = (a); \
+	typeof(b) __b = (b); \
+	typeof(*(d)) __sum = __a + __b; \
+	*(d) = __sum; \
+	__sum < __a; \
+})
+#endif
+
+#ifndef array_size
+#define array_size(a, b) ((a) * (b))
+#endif
+
+#ifndef struct_size
+#define struct_size(p, member, n) \
+	(sizeof(*(p)) + array_size((n), sizeof(*(p)->member)))
+#endif
+
+#endif
diff --git a/include/linux/pgtable.h b/include/linux/pgtable.h
new file mode 100644
index 000000000..b7d2049aa
--- /dev/null
+++ b/include/linux/pgtable.h
@@ -0,0 +1,6 @@
+#ifndef _LINUX_PGTABLE_H
+#define _LINUX_PGTABLE_H
+
+#include <asm/pgtable.h>
+
+#endif
diff --git a/include/linux/sched/signal.h b/include/linux/sched/signal.h
new file mode 100644
index 000000000..37c542d02
--- /dev/null
+++ b/include/linux/sched/signal.h
@@ -0,0 +1,7 @@
+#ifndef _LINUX_SCHED_SIGNAL_H
+#define _LINUX_SCHED_SIGNAL_H
+
+#include <linux/sched.h>
+#include <linux/signal.h>
+
+#endif
diff --git a/include/linux/sched/task.h b/include/linux/sched/task.h
new file mode 100644
index 000000000..3db3de9e8
--- /dev/null
+++ b/include/linux/sched/task.h
@@ -0,0 +1,6 @@
+#ifndef _LINUX_SCHED_TASK_H
+#define _LINUX_SCHED_TASK_H
+
+#include <linux/sched.h>
+
+#endif
diff --git a/include/linux/sched/task_stack.h b/include/linux/sched/task_stack.h
new file mode 100644
index 000000000..4497dd0fd
--- /dev/null
+++ b/include/linux/sched/task_stack.h
@@ -0,0 +1,6 @@
+#ifndef _LINUX_SCHED_TASK_STACK_H
+#define _LINUX_SCHED_TASK_STACK_H
+
+#include <linux/sched.h>
+
+#endif
diff --git a/include/linux/sched/user.h b/include/linux/sched/user.h
new file mode 100644
index 000000000..d5a9ebc23
--- /dev/null
+++ b/include/linux/sched/user.h
@@ -0,0 +1,6 @@
+#ifndef _LINUX_SCHED_USER_H
+#define _LINUX_SCHED_USER_H
+
+#include <linux/sched.h>
+
+#endif
